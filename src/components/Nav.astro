<header class="nav">
  <div class="container nav-inner">
    <a href="/" class="brand">robium<em>.ai</em></a>
    <nav class="links">
      <a href="#plugin">Plugin</a>
      <a href="#skills">Skills</a>
      <a href="#demos">Demos</a>
      <a href="#faq">FAQ</a>
      <a href="https://github.com/robium-ai/robium-plugin" aria-label="GitHub" class="gh">
        <svg width="19" height="19" viewBox="0 0 24 24" fill="currentColor"><path d="M12 .5A11.5 11.5 0 0 0 .5 12a11.5 11.5 0 0 0 7.86 10.93c.58.1.79-.25.79-.56v-2.17c-3.2.7-3.87-1.37-3.87-1.37-.53-1.33-1.28-1.69-1.28-1.69-1.05-.72.08-.7.08-.7 1.16.08 1.77 1.19 1.77 1.19 1.03 1.77 2.7 1.26 3.36.96.1-.75.4-1.26.73-1.55-2.55-.29-5.23-1.28-5.23-5.68 0-1.26.45-2.28 1.19-3.09-.12-.29-.52-1.46.11-3.05 0 0 .97-.31 3.18 1.18a11 11 0 0 1 5.78 0c2.2-1.49 3.17-1.18 3.17-1.18.63 1.59.23 2.76.11 3.05.74.81 1.19 1.83 1.19 3.09 0 4.41-2.69 5.38-5.25 5.67.41.36.78 1.05.78 2.13v3.16c0 .31.2.67.8.55A11.5 11.5 0 0 0 23.5 12 11.5 11.5 0 0 0 12 .5z"/></svg>
      </a>
      <button id="theme-toggle" type="button" aria-label="Toggle light/dark theme">
        <svg class="icon-sun" width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><circle cx="12" cy="12" r="4.5"/><path d="M12 2v2.5M12 19.5V22M2 12h2.5M19.5 12H22M4.9 4.9l1.8 1.8M17.3 17.3l1.8 1.8M19.1 4.9l-1.8 1.8M6.7 17.3l-1.8 1.8"/></svg>
        <svg class="icon-moon" width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M21 13.3A8.6 8.6 0 0 1 10.7 3 8.9 8.9 0 1 0 21 13.3z"/></svg>
      </button>
    </nav>
  </div>
</header>

<style>
  .nav {
    position: sticky; top: 0; z-index: 10;
    background: color-mix(in srgb, var(--bg) 92%, transparent);
    backdrop-filter: blur(10px);
    border-bottom: 3px solid var(--accent);
  }
  .nav-inner { display: flex; align-items: center; gap: 28px; height: 58px; }
  .brand {
    font-family: var(--mono); font-weight: 700; font-size: 17px;
    letter-spacing: -0.02em; color: var(--ink); text-decoration: none;
  }
  .brand em { font-style: normal; color: var(--accent-ink); }
  .links { display: flex; align-items: center; gap: 22px; margin-left: auto; }
  .links a { color: var(--ink2); text-decoration: none; font-size: 14px; transition: color 150ms; }
  .links a:hover { color: var(--ink); }
  .gh { display: flex; }

  #theme-toggle {
    display: flex; align-items: center; justify-content: center;
    width: 32px; height: 32px; padding: 0;
    border: 1px solid var(--border2); border-radius: 8px;
    background: var(--bg); color: var(--ink2); cursor: pointer;
    transition: color 150ms, border-color 150ms;
  }
  #theme-toggle:hover { color: var(--ink); border-color: var(--ink); }
  /* moon in light mode (click → dark), sun in dark mode (click → light) */
  #theme-toggle .icon-sun { display: none; }
  :global(html[data-theme='dark']) #theme-toggle .icon-sun { display: block; }
  :global(html[data-theme='dark']) #theme-toggle .icon-moon { display: none; }

  @media (max-width: 640px) { .links a:not(.gh) { display: none; } }
</style>

<script>
  // Theme switch: flip html[data-theme], persist the choice. The pre-paint
  // script in Base.astro applies it on the next load.
  document.getElementById('theme-toggle')?.addEventListener('click', () => {
    const root = document.documentElement;
    const next = root.dataset.theme === 'dark' ? 'light' : 'dark';
    root.dataset.theme = next;
    try { localStorage.setItem('theme', next); } catch {}
  });
</script>
